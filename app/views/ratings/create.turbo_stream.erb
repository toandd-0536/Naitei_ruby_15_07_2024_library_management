<%= turbo_stream.replace "ratings_form" do %>
  <%= render partial: "ratings/my_review", locals: { rating: @episode.ratings.find_by(user: current_user) } %>
<% end %>

<%= turbo_stream.replace "ratings" do %>
  <%= render partial: "ratings/ratings_div", user_rating: @episode.ratings.find_by(user: current_user), episode: @episode %>
<% end %>
