<div class="modal fade" id="advancedSearchModal" tabindex="-1" aria-labelledby="advancedSearchModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="advancedSearchModalLabel">Advanced Search</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <%= search_form_for @q, url: admin_books_path, method: :get, html: { class: "advanced-search-form", id: "advancedSearchForm" } do |f| %>
          <div class="mb-3">
            <%= f.label :name, t(".name") %>
            <%= f.text_field :name_cont, class: "form-control" %>
          </div>
          <div class="mb-3">
            <%= f.label :categories, t("admin.books.new.cat") %>
            <%= f.collection_select :categories_id_in, @categories, :id, :name, {}, { multiple: true, class: "form-control multi-select" } %>
          </div>
          <div class="mb-3">
            <%= f.label :authors, t("admin.books.new.author") %>
            <%= f.collection_select :authors_id_in, @authors, :id, :name, {}, { multiple: true, class: "form-control multi-select" } %>
          </div>
          <div class="d-flex justify-content-between">
            <%= button_tag t("admin.form.reset"), type: :button, class: "btn btn-secondary", id: "resetButton" %>
            <%= f.submit t("admin.form.search"), class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
